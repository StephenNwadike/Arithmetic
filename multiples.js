var ans=0;

for(var i=0; i<=1000; i++);
if (i%3 === 0)
{
	ans+=i;
	console.log("Sum of multiples of 3 =" + ans);
	return ans;
}
else if (i%5 === 0)
{
	ans+=i;
	Console.log("Sum of multiples of 3 and 5 =" + ans);
	return ans;
}
else{ return ans; };